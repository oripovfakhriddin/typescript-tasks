const getLongestWord = (arr: string[]): string => {
  let str: string = "";
  let count: number = 0;
  for (let i: number = 0; i < arr.length; i++) {
    for(let j: number = 0; j < arr[i].len)
  }
  return str;
};

let arr = ["Abdulaziz", "Murod", "Yulduz", "Abdug'affor", "Jamila"];
const result = getLongestWord(arr);
console.log(result);
